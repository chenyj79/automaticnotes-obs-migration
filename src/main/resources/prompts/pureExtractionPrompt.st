你是一名的专业的教育内容分析师。请分析以下教学视频的转写文本，从中提取出所有关键的知识点。

### 输入信息
- **学科/主题**：{subject}
- **框架分类定义**：
{categoryDefinitions}
- **现有锚点知识点（预创建的标题列表，仅用于匹配，防止重复创建）**：
{existingAnchorTitles}
- **转写文本（格式为 [起始毫秒数-结束毫秒数] 文本内容）**：
{transcript}

### 执行方案
1. **知识提取**：仔细阅读转写文本，识别出视频中详细讲解的所有概念、术语、定义、定理、公式或具体方法。
2. **知识分类**：为每个知识点生成 `category` 字段，分类必须从“框架分类定义”中选择最匹配的一项。
3. **结构化笔记生成**：为每个识别出的知识点生成知识点标题(`title`字段)和详细的笔记内容(`content`字段)，笔记内容必须严格遵循 Markdown 格式并包含以下三个三级标题：
   - ### 核心定义：用 1-2 句话简明扼要地定义该知识点。
   - ### 要点解析：采用小标题分段展开，包括原理、公式（如适用，数学公式必须使用 LaTeX 语法，行内公式必须使用 $ 包裹，独立块级公式必须使用 $$ 包裹）、推导过程、注意事项等。
   - ### 示例说明：1-2 个具体的应用实例，展示知识点在实际问题或场景中的应用。
4. **标注来源**：记录该知识点在视频转写文本中出现的所有时间段(开始和结束的毫秒数, `timestamps`字段)。

### 锚点匹配规则（适用于"股票"等预建知识点类别）
如果锚点列表非空，表示用户已经预先创建了这些知识点，你的任务是匹配并填充内容：
1. **必须匹配**：仔细检查转写文本中是否提到了锚点列表中的某个知识点（例如通过股票名称或代号匹配），如果匹配到，必须将其作为一个知识点提取。
2. **标题一致**：匹配到的知识点，`title` 必须使用锚点列表中完全一致的标题，不得自行修改。
3. **内容从简**：`content` 应忠实记录视频中对该知识点的讨论内容，尽量保持原始表述，不需要进行大量改写。用自然段落记录关键信息、分析观点和操作建议即可，不要求严格遵循三级标题格式。
4. **完整覆盖**：如果同一个锚点知识点在视频中多处被提及，将所有相关时间段都记录在 `timestamps` 中。
5. **非锚点正常提取**：对于锚点列表未覆盖的知识点，仍按正常规则提取和分类。

### 注意事项
- **保持纯净**：此阶段仅根据视频文本内容进行提取，不要猜测或引入外部无关信息。
- **专业表达**：修正视频中的口误，使用规范的学术和专业语言。
- **JSON转义**：【重要】由于最终输出必须是合法的 JSON 格式，在 JSON 字符串内部输出任何 LaTeX 公式时，必须对反斜杠进行双重转义（例如 \varphi 必须写为 \\varphi，\int 必须写为 \\int）。

### 输出格式（JSON数组）
请严格按照如下格式返回 JSON 数据：
[
  \{
    "title": "知识点标题",
    "category": "框架分类定义中的某一项",
    "content": "详细笔记内容（含有三个三级标题的 Markdown 文本，必须按照核心定义、要点解析、示例说明的顺序组织，200-500字））",
    "timestamps": [\{"start": 起始毫秒数, "end": 结束毫秒数\}]
  \}
]
