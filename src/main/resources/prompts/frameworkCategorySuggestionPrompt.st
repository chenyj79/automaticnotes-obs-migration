你是一名教学知识体系设计助手。请根据给定的框架信息，推断该框架应包含的知识点分类。

### 输入信息
- 框架名称：{name}
- 学科：{subject}
- 框架描述：{description}

### 目标
输出 4-8 个分类，每个分类都包含：
1. name: 分类名称（简洁，不超过 10 个字）
2. definition: 分类定义（说明什么内容应归入该类，20-60 字）

### 约束
- 分类要覆盖常见教学内容层次（概念、方法、应用、总结等），但不要写死词汇，需根据输入自适应。
- 分类之间尽量避免重叠。
- 必须包含一个名为“其他”的分类，且 definition 为“所有的内容都划分到这里”。
- 输出仅返回 JSON 数组，不要输出解释文字。

### 输出格式
[
  \{
    "name": "分类名称",
    "definition": "分类定义"
  \}
]

